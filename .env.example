# DeepSeek API key (required)
DEEPSEEK_API_KEY=sk-your-key-here

# Optional: override the base URL if using a proxy or self-hosted instance
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# Output directory for CRM CSV exports (defaults to ./output)
# EMAIL_AGENT_OUTPUT_DIR=./output
